# ===========================================
# RecordRoute 환경 설정
# ===========================================
# 이 파일을 .env로 복사하여 사용하세요: cp .env.example .env
# 자세한 설정 가이드: recordroute-rs/CONFIGURATION.md

# IMPORTANT: 모든 상대 경로는 프로젝트 루트 (RecordRoute/) 기준입니다.
# RecordRoute는 자동으로 .git 디렉토리를 찾아 프로젝트 루트를 결정합니다.

# --- Whisper STT ---
# Whisper 모델 파일 경로 (프로젝트 루트 기준 상대 경로 또는 절대 경로)
# 모델 파일은 프로젝트 루트의 models/ 폴더에 배치하세요
WHISPER_MODEL=./models/ggml-base.bin

# --- Ollama ---
# Ollama API 서버 URL
OLLAMA_BASE_URL=http://localhost:11434

# LLM 모델 (요약용)
LLM_MODEL=llama3.2

# 임베딩 모델 (검색용)
EMBEDDING_MODEL=nomic-embed-text

# --- 데이터 경로 ---
# 데이터베이스 기본 경로 (프로젝트 루트 기준 상대 경로)
DB_BASE_PATH=./recordroute-rs/db

# 파일 업로드 디렉토리
UPLOAD_DIR=./recordroute-rs/db/uploads

# 벡터 인덱스 파일
VECTOR_INDEX_PATH=./recordroute-rs/db/vector_index.json

# --- 서버 ---
# 서버 바인드 주소
SERVER_HOST=0.0.0.0

# 서버 포트
SERVER_PORT=8080

# WebSocket 서버 포트 (실시간 업데이트용)
WEBSOCKET_PORT=8765

# --- 로그 ---
# 로그 디렉토리
LOG_DIR=./recordroute-rs/db/log

# 로그 레벨 (trace, debug, info, warn, error)
LOG_LEVEL=info
