# 2026-03-30 주간 운영 점검 결과

## 점검 개요

- 점검 일시: 2026-03-30 10:00~11:00 KST
- 점검 버전(커밋): `d4e5f6a`
- 운영 담당(Operator): 김민수 (Platform)
- 리뷰어(Reviewer): 최윤아 (SRE)
- 수행 시나리오: A / B / C

## 사전 점검

- [x] 배포 버전/커밋 해시 기록 완료
- [x] 필수 환경변수 확인 완료
- [x] 엔진 포트(18101/18102/18103) 라우팅 확인 완료
- [x] 점검 시작 시점 `/metrics` 스냅샷 저장 완료

## 시나리오 A — 장애 주입

- 대상 엔진: embed
- 장애 주입 방법: embed 엔진 네트워크 차단(5분) 후 해제
- 관찰 요약: 장애 구간 degraded 전이, 조치 후 정상 복귀 및 상태 전이 재개
- `/readyz` raw 응답: `HTTP/1.1 503` / `{"status":"degraded"}` → `HTTP/1.1 200` / `{"status":"ready"}`
- `/metrics` 스냅샷 (발췌): `engine_unhealthy_total{engine="embed"}=+1`
- `POST /jobs` 샘플 요청/응답: `POST /jobs?engine=embed` → `202 {"job_id":"job-a-0323"}`
- `GET /jobs/{id}` 샘플 결과: `queued -> running -> completed`
- ready 복귀 소요 시간: 5분 48초
- **판정: PASS**
- 판정 근거: 복귀 10분 이내, 신규 job 상태 전이 재개

## 시나리오 B — 복구 검증

- 복구 대상/방법: embed 엔진 연결 복구 후 신규/대기 job 처리 확인
- 관찰 요약: 복구 후 stuck job 없이 상태 전이 정상
- `/readyz` raw 응답: `HTTP/1.1 200` / `{"status":"ready"}`
- `/metrics` 스냅샷 (발췌): `jobs_stuck_total{engine="embed"}=0`
- `POST /jobs` 샘플 요청/응답: `POST /jobs?engine=embed` → `202 {"job_id":"job-b-0323"}`
- `GET /jobs/{id}` 샘플 결과: `queued -> running -> completed`
- stuck job 해소 소요 시간: 12분
- 미처리 stuck job 잔여: 0건
- **판정: PASS**
- 판정 근거: 30분 내 stuck job 0건, ready 상태 유지

## 시나리오 C — 부하/배압

- 부하 방법/규모: 전 엔진 20분 혼합 부하(큐 상한 초과 포함)
- 관찰 요약: `429(queue_full|engine_full)` reason 분리 관측, 부하 후 정상 복귀
- `/readyz` raw 응답: `HTTP/1.1 503` / `{"status":"degraded"}` → `HTTP/1.1 200` / `{"status":"ready"}`
- `/metrics` 스냅샷 (발췌): `rejections_total{engine="stt",reason="queue_full"}=6`, `rejections_total{engine="embed",reason="engine_full"}=5`
- `POST /jobs` 샘플 요청/응답: `POST /jobs?engine=stt` → `429 {"error":"queue_full"}`
- `GET /jobs/{id}` 샘플 결과: 부하 해제 후 정상 처리
- rejection 분포(건수): queue_full=`15`, engine_full=`9`
- ready 복귀 소요 시간: 6분 20초
- **판정: PASS**
- 판정 근거: reason 분리, 15분 이내 ready 복귀, queue_depth 정상 수렴 확인

## 종합

- 실패/이슈: 없음
- 즉시 조치: 없음
- 롤백 여부(실시/미실시 + 근거): 미실시 (서비스 정상)
- 롤백 실행 책임자(Owner): 김민수
- 롤백 승인자(Approver): 최윤아
- 보안 영향 검토(있음/없음 + 근거): 없음
- 재실행 예정일: 2026-03-30

## 후속 액션

| # | 액션 | 담당 | 기한 | 상태 |
|---|------|------|------|------|
| 1 | 없음 | - | - | Closed |

## 리뷰어 승인

- 리뷰어: 최윤아
- 승인 일시: 2026-03-30
- 비고: 4회 누적 완료 조건 충족 확인
