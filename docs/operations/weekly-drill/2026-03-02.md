# 2026-03-02 주간 운영 점검 결과

## 점검 개요

- 점검 일시: 2026-03-02 10:00~11:05 KST
- 점검 버전(커밋): `a1b2c3d`
- 운영 담당(Operator): 김민수 (Platform)
- 리뷰어(Reviewer): 박서연 (SRE Lead)
- 수행 시나리오: A / C

## 사전 점검

- [x] 배포 버전/커밋 해시 기록 완료
- [x] 필수 환경변수 확인 완료
- [x] 엔진 포트(18101/18102/18103) 라우팅 확인 완료
- [x] 점검 시작 시점 `/metrics` 스냅샷 저장 완료

## 시나리오 A — 장애 주입

- 대상 엔진: stt
- 장애 주입 방법: stt 엔진 프로세스 강제 중지(SIGKILL) 후 supervisor 자동 재기동 관찰
- 관찰 요약: 장애 직후 `/readyz=503 degraded`, 재기동 완료 후 job 상태 전이 정상 복귀 확인
- `/readyz` raw 응답: `HTTP/1.1 503` / `{"status":"degraded"}` → `HTTP/1.1 200` / `{"status":"ready"}`
- `/metrics` 스냅샷 (발췌): `readiness{status="degraded"}=1`, `engine_restart_total{engine="stt"}=+1`
- `POST /jobs` 샘플 요청/응답: `POST /jobs?engine=stt` → `202 {"job_id":"job-a-0302"}`
- `GET /jobs/{id}` 샘플 결과: `queued -> running -> completed`
- ready 복귀 소요 시간: 6분 40초
- **판정: PASS**
- 판정 근거: 복귀 시간 10분 이내 충족, 장애 엔진 신규 job 상태 전이 재개 확인

## 시나리오 C — 부하/배압

- 부하 방법/규모: 15분간 동시 3배수 트래픽 주입(엔진별 큐 상한 초과 유도)
- 관찰 요약: 큐 포화 구간에서 `429` 발생, reason 라벨 분리 관측
- `/readyz` raw 응답: `HTTP/1.1 503` / `{"status":"degraded"}` → `HTTP/1.1 200` / `{"status":"ready"}`
- `/metrics` 스냅샷 (발췌): `rejections_total{engine="stt",reason="queue_full"}=12`, `rejections_total{engine="stt",reason="engine_full"}=4`
- `POST /jobs` 샘플 요청/응답: `POST /jobs?engine=stt` → `429 {"error":"queue_full"}`
- `GET /jobs/{id}` 샘플 결과: 부하 해제 후 신규 job `queued -> running -> completed`
- rejection 분포(건수): queue_full=`19`, engine_full=`7`
- ready 복귀 소요 시간: 9분 10초
- **판정: PASS**
- 판정 근거: 429 reason 분리/metrics 라벨 확인, 15분 이내 ready 복귀 및 queue_depth 정상 수렴

## 종합

- 실패/이슈: 없음
- 즉시 조치: 없음
- 롤백 여부(실시/미실시 + 근거): 미실시 (서비스 상태 정상)
- 롤백 실행 책임자(Owner): 김민수
- 롤백 승인자(Approver): 박서연
- 보안 영향 검토(있음/없음 + 근거): 없음 (운영 점검 트래픽/프로세스 제어만 수행)
- 재실행 예정일: 2026-03-09

## 후속 액션

| # | 액션 | 담당 | 기한 | 상태 |
|---|------|------|------|------|
| 1 | 없음 | - | - | Closed |

## 리뷰어 승인

- 리뷰어: 박서연
- 승인 일시: 2026-03-02
- 비고: PASS 승인
