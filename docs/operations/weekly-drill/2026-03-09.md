# 2026-03-09 주간 운영 점검 결과

## 점검 개요

- 점검 일시: 2026-03-09 10:00~11:20 KST
- 점검 버전(커밋): `b2c3d4e`
- 운영 담당(Operator): 이정훈 (Platform)
- 리뷰어(Reviewer): 박서연 (SRE Lead)
- 수행 시나리오: B / C

## 사전 점검

- [x] 배포 버전/커밋 해시 기록 완료
- [x] 필수 환경변수 확인 완료
- [x] 엔진 포트(18101/18102/18103) 라우팅 확인 완료
- [x] 점검 시작 시점 `/metrics` 스냅샷 저장 완료

## 시나리오 B — 복구 검증

- 복구 대상/방법: summarize 엔진 재기동 후 backlog drain 관찰
- 관찰 요약: 신규 요청 상태 전이는 재개되었으나 stuck job 2건이 30분 내 해소되지 않음
- `/readyz` raw 응답: `HTTP/1.1 200` / `{"status":"ready"}`
- `/metrics` 스냅샷 (발췌): `jobs_stuck_total{engine="summarize"}=2` (30분 시점)
- `POST /jobs` 샘플 요청/응답: `POST /jobs?engine=summarize` → `202 {"job_id":"job-b-0309"}`
- `GET /jobs/{id}` 샘플 결과: `queued -> running -> completed`
- stuck job 해소 소요 시간: 42분
- 미처리 stuck job 잔여: 2건 (30분 시점)
- **판정: FAIL**
- 판정 근거: 30분 내 stuck job 0건 조건 미충족

## 시나리오 C — 부하/배압

- 부하 방법/규모: 10분간 summarize 엔진 집중 부하 + 혼합 엔진 트래픽
- 관찰 요약: `429` reason 라벨은 정상 분리, 부하 해제 후 정상 복귀
- `/readyz` raw 응답: `HTTP/1.1 503` / `{"status":"degraded"}` → `HTTP/1.1 200` / `{"status":"ready"}`
- `/metrics` 스냅샷 (발췌): `rejections_total{engine="summarize",reason="queue_full"}=9`, `rejections_total{engine="summarize",reason="engine_full"}=3`
- `POST /jobs` 샘플 요청/응답: `POST /jobs?engine=summarize` → `429 {"error":"engine_full"}`
- `GET /jobs/{id}` 샘플 결과: 부하 해제 후 신규 job 정상 처리
- rejection 분포(건수): queue_full=`14`, engine_full=`5`
- ready 복귀 소요 시간: 8분 55초
- **판정: PASS**
- 판정 근거: C 시나리오 기준 충족

## 종합

- 실패/이슈: 시나리오 B FAIL (stuck job 청산 지연)
- 즉시 조치: backlog 재처리 스크립트 수동 실행
- 롤백 여부(실시/미실시 + 근거): 미실시 (서비스 중단 없음)
- 롤백 실행 책임자(Owner): 이정훈
- 롤백 승인자(Approver): 박서연
- 보안 영향 검토(있음/없음 + 근거): 없음 (복구/부하 시나리오만 수행)
- 재실행 예정일: 2026-03-16 (B 재검증 포함)

## 후속 액션

| # | 액션 | 담당 | 기한 | 상태 |
|---|------|------|------|------|
| 1 | summarize stuck job 자동 청산 임계치 조정(30분 내 0건 보장) | 이정훈 | 2026-03-15 | Open |
| 2 | stuck job 알람 임계치(15분) 추가 및 온콜 라우팅 연동 | 박서연 | 2026-03-15 | Open |

## 리뷰어 승인

- 리뷰어: 박서연
- 승인 일시: 2026-03-09
- 비고: 회차 전체 판정 FAIL, 액션 아이템 등록 승인
