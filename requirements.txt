# PyTorch CUDA wheels (Windows에서 GPU 가속을 위해 추가 인덱스)
--extra-index-url https://download.pytorch.org/whl/cu124

# GPU 가속을 위해 torch 패키지 3종을 CUDA 버전으로 명시적 지정
# (이들이 없으면 의존성 자동 설치 시 CPU 버전이 깔려 충돌할 수 있습니다)
torch
torchaudio
torchvision

# 나머지 의존성
openai-whisper>=20231117
multipart
python-dotenv
sentence-transformers
pypdf>=3.0.0
websockets>=10.0

# Obsidian MCP 통합
mcp>=0.1.0

# Optional provider dependencies
# - Ollama provider를 사용하려면 아래 파일을 추가 설치하세요.
#   pip install -r requirements-ollama.txt

llama-cpp-python
