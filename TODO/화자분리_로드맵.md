# 화자 분리(Speaker Diarization) 실행 로드맵

- 문서 목적: RecordRoute에 화자 분리를 **기존 계약을 깨지 않고** 도입하기 위한 실행 백로그
- 기준 문서 반영: `README.md`, `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`
- 작성 원칙: TODO 관례에 맞춰 **실행 항목/완료 기준(DoD)/리스크** 중심으로 유지
- 기본 엔진 가정: 1차 화자 분리 엔진은 `pyannote.audio` 기반 `pyannote`(Hugging Face 토큰 기반 인증, `PYANNOTE_TOKEN`)을 기본으로 적용한다.

## 코드베이스 현황 스냅샷 (2026-02-19 점검, 구현/미구현 분리)

- `/process`의 `steps`에서 `diarize`를 인식하며, 비오디오 입력은 `status: "skipped"`로 처리됨.
- `model_settings.diarization_provider/num_speakers/min_speakers/max_speakers` 검증 로직이 백엔드에 반영되어 있음.
- STT 세그먼트(`*.segments.json`)와 `/process` 응답(`stt_segments`)에서 `speaker` 필드가 유지됨.
- 프론트는 API 타입(`frontend/src/api/types.ts`)에 diarization 계약이 반영되어 있으나, UI 렌더링(화자 배지/필터)은 아직 미구현.
- 현재 diarization 실행은 **계약 안정화용 baseline payload**(단일 `SPEAKER_00`) 중심이며, 실제 pyannote 추론 파이프라인 연결은 후속 작업이 필요.
- 즉, 본 문서의 완료 표시는 "계약/스키마/호환성 반영" 관점이며 "모델 품질/운영 완성"과는 분리해 해석한다.

---

## 0) 목표 / 비목표

### 목표
- `/process` 워크플로우에 화자 분리 결과를 추가해 STT 결과(`segments`)에 `speaker`를 반영한다.
- 실패 시에도 기존 사용자 흐름(STT/교정/요약/검색)이 깨지지 않도록 backward compatibility를 보장한다.
- 프론트(`frontend/src`)에서 화자 라벨을 읽고 표시할 수 있게 API 타입/클라이언트를 동기화한다.

### 비목표(초기)
- 실명 화자 식별(voice fingerprint)
- 실시간 스트리밍 diarization 완성형
- 복수 겹화자(overlap) 고급 라벨링(초기에는 단일 주 화자 정책)

---

## 1) 아키텍처 반영 지점(필수)

- 워크플로우 실행: `sttEngine/http_api/workflow.py`
- 라우팅/요청 처리: `sttEngine/http_api/handler.py`, `sttEngine/http_api/routes/*`
- provider 계층: `sttEngine/providers/*`, `sttEngine/llm_provider.py`
- 프론트 API 동기화: `frontend/src/api/client.ts`, `frontend/src/api/types.ts`
- 데이터 경로 규칙: `DB/...` alias + `sttEngine/http_api/paths.py` 유틸 사용

---

## 2) 단계별 계획(Phase 0~4)

## Phase 0. 계약/지표 고정 (1주)

### TODO
- [x] 화자 분리 성공 기준 확정: `DER`, 화자 전환점 오차, 요약 품질 영향
- [x] 샘플 데이터셋(1:1, 3~5인, 잡음환경) 선정 및 보관 위치 확정
- [x] 실패 정의(모델 불가/타임아웃/오디오 무효) 및 공통 에러코드 초안 작성

### DoD
- [x] 합의된 KPI 문서 1건 (`docs/diarization/kpi.md`)
- [x] 재현 가능한 평가 입력셋 목록 1건 (`docs/diarization/dataset-manifest.md`)

---

## Phase 1. 백엔드 MVP (1~2주)

### TODO
- [x] `/process`에서 `steps` 확장안 적용
  - 권장: `"diarize"` step 추가(기존 step 미변경 요청도 100% 호환)
- [x] `model_settings` 확장
  - 예: `diarization_provider`, `num_speakers`, `min_speakers`, `max_speakers`
  - `diarization_provider` 기본값은 `pyannote`를 우선 지원하도록 구현한다(차후 provider 추상화로 확장).
- [x] STT 결과 스키마 확장
  - `segments[].speaker`(`SPEAKER_00` 형식)
- [x] 진행률 반영
  - `/progress/<task_id>` 및 WS에서 diarization 단계 표시
- [x] 실패 응답 규약 유지
  - `error`, `error_code`, `retryable`, `failed_step`

### DoD
- [x] 기존 요청 payload(화자 옵션 없음) 호환 경로 유지
- [x] 화자 옵션 요청 시 `segments[].speaker` 계약 노출
- [ ] pyannote 실추론 연동 기준에서 품질 KPI 충족(DER/F1)

---

## Phase 2. 저장/호환/복구 (1주)

### TODO
- [x] 저장 포맷에 화자 정보 nullable로 추가
  - 과거 데이터(화자 없음) 로드 경로 유지
- [x] fallback 정책 적용
  - diarization 실패 시 STT 텍스트는 반환, 화자 라벨만 비활성화
- [x] 표준 오류코드 확정
  - `diarization_model_unavailable`
  - `diarization_timeout`
  - `diarization_invalid_audio`

### DoD
- [x] 기존 DB 데이터로 UI/검색 회귀 없음
- [x] diarization 실패 케이스에서 사용자 오류카드 규약 유지
- 검증 메모: `tests/http_api/test_workflow.py`, `tests/http_api/test_search.py`, `tests/server/test_queue.py`, `tests/test_vocab_system.py`, `frontend npm build` 기준 통과.

---

## Phase 3. 프론트/UI 연동 (1~2주)

### TODO
- [x] 상세 화면 세그먼트에 화자 배지 표시
- [x] 화자 필터/묶음 보기 토글 추가
- [x] API 클라이언트/타입 동기화
  - `frontend/src/api/client.ts`
  - `frontend/src/api/types.ts`
- [ ] 요약 연계(선택)
  - 화자 정보 포함 요약 템플릿(불확실성 문구 포함)

### DoD
- [x] 화자 라벨이 있는/없는 레코드 모두 렌더 오류 0건
- [x] 프론트 빌드 성공

---

## Phase 4. 운영 안정화 (1주)

### TODO
- [x] 처리 시간/실패율 모니터링 지표 추가
  - `stt`, `diarize`, `correct`, `summary` 단계별 소요시간
- [x] 긴 오디오(60분+) 처리 정책 점검(큐/타임아웃)
- [x] 보존/삭제 정책 점검(화자 정보 민감정보 취급 여부 포함)

### DoD
- [x] 운영 지표 대시보드/로그에서 diarization 상태 추적 가능
- [x] 삭제/리셋 API와 충돌 없는 데이터 정리 검증


### 검증 메모 (2026-02-19)
- `/metrics/workflow` GET 추가: `stt/diarize/correct/summary` 단계별 `runs/successes/failures/failure_rate/avg_duration_seconds`를 조회 가능.
- 워크플로우 실행 시 `[METRIC]` 구조화 로그를 남겨 단계별 상태(`completed|failed`), 소요시간, `error_code`를 추적.
- diarization 단계에서 60분 이상 오디오 감지 시 `[POLICY] long_audio_detected ... queue_policy=single_worker timeout_policy=diarization_timeout` 로그를 남김.
- `reset_upload_record()` 경로에서 output 디렉터리(세그먼트 sidecar 포함) 제거 및 히스토리 상태 초기화 검증 테스트 추가.

---

## 3) 공통 회귀 테스트 체크리스트

- [ ] `pytest tests/http_api/test_workflow.py`
- [ ] `pytest tests/http_api/test_search.py` (검색 연계 변경 시)
- [ ] `pytest tests/server/test_queue.py`
- [ ] `pytest tests/test_vocab_system.py`
- [ ] (프론트 변경 시) `cd frontend && npm run build`

> 체크리스트는 "상시 실행 항목"으로 유지하며, 특정 커밋에서 일시 실행한 결과는 PR의 Testing 섹션에만 기록한다.

---

## 4) 현재 코드 기준 갭(다음 작업 우선순위)

1. **실제 diarization provider 연결(최우선)**
   - 현재 `run_diarize_step()`은 계약 유지용 baseline payload를 반환하므로, `pyannote` 실제 추론 연결/timeout/리소스 해제 정책이 필요.
2. **KPI 기준 품질 검증 자동화**
   - `docs/diarization/kpi.md`, `docs/diarization/dataset-manifest.md` 기준으로 DER/F1 리포트를 CI 또는 배치 스크립트로 자동 산출하는 경로가 아직 없음.
3. **요약 연계 고도화(선택 과제)**
   - 화자 라벨을 활용한 요약 템플릿(불확실성 문구 포함)은 아직 미완료 상태.
4. **운영 토글의 코드 레벨 명문화**
   - 문서상 롤백 전략으로 기능 플래그(`DIARIZATION_ENABLED`)를 언급하고 있으나, 현재 코드에는 해당 플래그 분기 구현이 확인되지 않음. 환경변수 기반 on/off 분기 추가 여부를 결정할 필요가 있음.
5. **프론트 회귀 테스트 보강**
   - 화자 배지/필터/묶음 보기 UI는 반영되었으나, 혼재 데이터(화자 있음/없음) 시나리오에 대한 자동화 테스트(컴포넌트 또는 E2E)가 부족함.

---

## 5) 리스크 / 롤백

### 주요 리스크
- 모델/의존성 설치 실패(환경별)
- 한국어 다화자 환경 정확도 편차
- 처리시간 증가로 인한 큐 적체

### 롤백 전략
- 기능 플래그(예: `DIARIZATION_ENABLED=false`)로 즉시 비활성화 *(현재 문서 전략이며, 코드 분기 구현 여부는 별도 확정 필요)*
- 저장 포맷은 nullable 유지로 기존 데이터/화면 호환 보장
- 실패 시 STT 텍스트 우선 반환 정책으로 사용자 흐름 보전

---

## 6) 제안 일정(총 4~7주)

- 1주차: Phase 0
- 2~3주차: Phase 1
- 4주차: Phase 2
- 5~6주차: Phase 3
- 7주차: Phase 4

> 출시 최소 조건(MVP):
> 1) 화자 라벨이 포함된 STT 결과 반환,
> 2) 실패 시 기존 흐름 유지,
> 3) 프론트에서 라벨 표시 가능.
