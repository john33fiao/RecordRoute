# TODO Master (코드 기준 재정렬)

- 마지막 점검일: 2026-02-13
- 대조 기준 파일: `sttEngine/http_api/app.py`, `sttEngine/http_api/handler.py`, `sttEngine/http_api/routes/*(현 구조에서는 sttEngine/server/routes/* 병행)`, `frontend/src/*`, `sttEngine/similarity_matrix.py`, `frontend/graph-view.html`
- 상태 정의: `완료 / 부분완료 / 미착수`

## 1) 코어 워크플로우

| 항목 | 상태 | 우선순위 | 의존관계 | 근거 |
|---|---|---|---|---|
| 업로드 → STT/교정/요약/임베딩 파이프라인 동작 | 완료 | - | - | `/upload`, `/process`, `/progress`, 큐 처리 및 단계 분기 구현 |
| 작업 취소/재시도 흐름 | 부분완료 | P1 | 작업 상태 표준화 | `/cancel` + 프론트 retry 존재, 실패 유형별 자동복구 정책 부재 |
| 모델 설정(전사/요약/임베딩) 변경 | 완료 | - | - | `/models` + 프론트 설정 다이얼로그/로컬 저장 |

## 2) 검색/탐색

| 항목 | 상태 | 우선순위 | 의존관계 | 근거 |
|---|---|---|---|---|
| 통합 검색(`/search`) + 파일 검색(`/file_search`) | 완료 | - | - | 필터/정렬/페이지네이션/점수 분해 응답 포함 |
| 검색 캐시/정리 API | 완료 | - | - | `/cache/stats`, `/cache/cleanup` |
| 검색 성능 기준선(벤치, 튜닝 문서) | 미착수 | P1 | API 계측 항목 고정 | 코드 상 자동 벤치 루틴/운영 가이드 부재 |
| 고급 필터 표준화(버전드 계약 포함) | 부분완료 | P1 | OpenAPI/스키마 문서화 | 현재 쿼리파라미터는 있으나 버전 전략 없음 |

## 3) API/서버 구조

| 항목 | 상태 | 우선순위 | 의존관계 | 근거 |
|---|---|---|---|---|
| 엔트리포인트 모듈화(`http_api/app.py`) | 완료 | - | - | 실행 진입점 분리 완료 |
| 대형 핸들러 책임 분리(`handler.py`) | 부분완료 | P0 | 라우트 단위 모듈 분해 | 다수 엔드포인트가 단일 클래스에 집중 |
| 에러 응답 규격 통일 | 부분완료 | P1 | 공통 에러 DTO 정의 | 엔드포인트별 응답 포맷/상태코드 편차 존재 |
| 인증/권한 | 미착수 | P0 | 배포 모드(Electron/서버) 정책 | 파괴적 API 보호 부재 |
| API 버전 관리/OpenAPI | 미착수 | P1 | 라우트 분리 선행 | 자동 문서/버전 라우팅 없음 |

## 4) 데이터/운영

| 항목 | 상태 | 우선순위 | 의존관계 | 근거 |
|---|---|---|---|---|
| 중복 파일 감지 | 완료 | - | - | 업로드 시 파일 해시 기반 중복 처리 |
| 스토리지 모니터링/자동정리/백업 | 미착수 | P2 | 데이터 경로 표준화 | 운영성 API/스케줄러 부재 |
| 처리 통계 대시보드용 집계 API | 미착수 | P2 | 이벤트 로깅 스키마 | 단건 진행 상태 위주, 집계 API 없음 |

## 5) 다음 스프린트 유효 백로그 (실행 순서)

1. **P0** `handler.py` 라우트 분해(검색/기록/관리/유사도/관리자 API 분리)
   - 의존: 기존 응답 스키마 고정, 회귀 테스트 최소 세트
2. **P0** 파괴적 엔드포인트 보호(`/shutdown`, `/delete*`, `/reset*`)
   - 의존: 토큰/세션 정책 결정
3. **P1** 검색 API 계약 정리(필터/정렬 파라미터 스펙 문서 + 버전 초안)
4. **P1** 실패 복구 정책(재시도 가능 오류 코드 표준화 + UI 가이드)
5. **P2** 운영 지표 API(스토리지, 처리량, 평균 소요시간)

---

## 제외/분리 메모

- 레거시 프론트(`frontend/legacy/upload.js` 모듈화)는 **GUI.md의 레거시 트랙**으로 분리 관리.
- React 전환 이후 backlog는 `frontend/src/*` 기준으로만 신규 추가.
