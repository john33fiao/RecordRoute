# GUI 백로그 (React 전환 기준)

- 마지막 점검일: 2026-02-16
- 대조 기준 파일: `frontend/src/*`, `sttEngine/http_api/state.py`, `sttEngine/http_api/ws.py`

## 0) 트랙 분리

### A. 레거시 트랙 (유지보수 전용)
> `frontend/legacy/*` 대상. 신규 기능 개발 대상 아님.

| 항목 | 상태 | 우선순위 | 의존관계 |
|---|---|---|---|
| `upload.js` 모듈화 완결/정리 | 미착수 | P2 | 레거시 제거 일정 확정 |
| 레거시 화면 버그 핫픽스만 허용 | 진행중(운영정책) | P2 | React 배포 안정성 |

### B. React 트랙 (유효 백로그)
> `frontend/src/*` 대상. 다음 스프린트 실행 대상.

## 1) 현재 구현 상태 요약 (React)

| 영역 | 상태 | 근거 |
|---|---|---|
| 업로드 드래그앤드롭 + 다중 파일 선택/미리목록 | 완료 | `UploadSection`에서 DnD, 파일 검증/중복 제외, 목록 렌더링 |
| 작업 큐/정렬/취소/재시도 + 진행률/ETA/오류 카드 | 완료 | `JobQueue`, `useTaskQueue`, `useWebSocket`에 `progress_percent`, `eta_seconds`, 오류 정보 반영 |
| 실시간 갱신(WebSocket + 폴링 폴백) | 부분완료 | 동시 운용 중이며 실패 복구 정책 표준화 미완료 |
| 검색 UI(질의 + 결과 + 유사문서 다이얼로그) | 부분완료 | 기본 검색은 안정화, 고급 필터 UX/하이라이트 부족 |
| 텍스트 오버레이(열람/복사/수정/삭제) | 부분완료 | 핵심 액션 구현 + Radix Dialog 사용, 접근성 회귀 테스트 미정리 |
| 설정/테마 | 부분완료 | 모델 설정 저장은 있음, 테마 영속화(localStorage) 부재 |

## 2) 다음 스프린트 유효 백로그

| 항목 | 상태 | 우선순위 | 의존관계 |
|---|---|---|---|
| 오버레이 접근성 회귀 테스트(ESC/포커스 트랩/ARIA) | 미착수 | P1 | 공통 Dialog 규약 |
| 검색 고급필터 UI(기간/최소점수/정렬) 정식 노출 | 부분완료 | P1 | 검색 API 계약 문서화 |
| 검색 결과 하이라이트/스니펫 | 미착수 | P1 | 백엔드 스니펫 응답 또는 프론트 하이라이트 규칙 |
| ThemeContext 영속화(localStorage + 시스템 테마 감지) | 미착수 | P1 | 디자인 토큰 정리 |
| Context 리렌더 최적화(AppContext 분리) | 미착수 | P2 | 프로파일링 리포트 |
| 대용량 히스토리 가상 스크롤 | 미착수 | P2 | 목록 아이템 컴포넌트 분리 |
| 훅/컴포넌트 테스트 추가(`useTaskQueue`, `SearchPanel`, `TextOverlay`) | 미착수 | P1 | 테스트 러너/픽스처 정비 |

## 3) 실행 순서 제안

1. **P1** 검색 고급화(필터/하이라이트)
2. **P1** 접근성 회귀 테스트와 실패 복구 UX 정리
3. **P1** 테마 영속화 + 테스트 보강
4. **P2** 성능 최적화(리렌더/가상 스크롤)
